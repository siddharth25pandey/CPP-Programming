class Solution {
public:
    string longestCommonPrefix(vector<string>& strs) {
        string x = "";
        for(int i=0;i<strs[0].size();i++){
            char ans=strs[0][i];
            for(int j=1;j<strs.size();j++){
                if(ans!=strs[j][i]){
                    cout<< x;
                    break;
                } 
            }
            x=x+ans;
        }
       // return x;
    }
};
